# disable datasource auto-configuration (no DB needed for this skeleton)
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# port for this service
server.port=8083
